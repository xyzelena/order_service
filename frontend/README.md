# Frontend - Order Service

Веб-интерфейс для поиска и отображения информации о заказах.

## 🎯 Описание

Современный веб-интерфейс для взаимодействия с Order Service API. Позволяет искать заказы по ID и отображает полную информацию в удобном формате.

## ✨ Функциональность

- **🔍 Поиск заказов** - поиск по order_uid с валидацией
- **📋 Детальная информация** - полная информация о заказе, доставке, платеже и товарах
- **📊 Дополнительные функции** - статистика кеша, список заказов, проверка здоровья сервиса
- **📱 Адаптивный дизайн** - корректное отображение на всех устройствах
- **⚡ Быстрый доступ** - автоматический fallback на базу данных при отсутствии в кеше
- **🎨 Современный UI** - красивый интерфейс с анимациями и градиентами

## 🛠️ Технологический стек

- **HTML5** - семантическая разметка
- **CSS3** - современные стили с CSS Grid и Flexbox
- **Vanilla JavaScript** - без фреймворков для простоты
- **Fetch API** - для HTTP запросов
- **CSS Variables** - для управления темой
- **Google Fonts** - шрифт Inter для современного вида

## 📁 Структура файлов

```
frontend/
├── index.html          # Главная страница
├── styles.css          # CSS стили и анимации
├── script.js           # JavaScript логика
└── README.md           # Документация
```

## 🚀 Быстрый старт

### Вариант 1: Простой HTTP сервер

```bash
# Python 3
cd frontend
python -m http.server 3000

# Или Python 2
python -m SimpleHTTPServer 3000

# Node.js (если установлен npx)
npx serve . -p 3000
```

### Вариант 2: Live Server (VS Code Extension)

1. Установите расширение "Live Server" в VS Code
2. Откройте `index.html`
3. Нажмите "Go Live" в статус-баре

### Вариант 3: Прямое открытие файла

Откройте `index.html` в браузере (некоторые функции могут не работать из-за CORS)

## 🔗 API Integration

Frontend взаимодействует с backend API:
- **Base URL**: http://localhost:8080/api/v1
- **CORS**: Включен для всех origins
- **Формат**: JSON API с единообразной структурой ответов

### Используемые эндпоинты:

| Эндпоинт | Описание |
|----------|----------|
| `GET /orders/{order_uid}` | Получение заказа по ID |
| `GET /orders?limit=10` | Список заказов |
| `GET /cache/stats` | Статистика кеша |
| `GET /health` | Проверка здоровья сервиса |

## 📋 Особенности интерфейса

### Главная форма поиска
- Валидация ввода order_uid
- Индикатор загрузки при выполнении запроса
- Автофокус на поле ввода
- Поддержка Enter для поиска

### Отображение заказа
- **Основная информация** - ID, трек-номер, клиент, дата
- **Доставка** - получатель, адрес, контакты
- **Платеж** - сумма, провайдер, банк, дата платежа
- **Товары** - название, бренд, цена, скидка, статус

### Дополнительные функции
- **Случайный заказ** - загрузка примера для демонстрации
- **Статистика кеша** - размер и использование кеша
- **Список заказов** - последние заказы с возможностью выбора
- **Проверка сервиса** - статус backend системы

### UX улучшения
- Красивые анимации при загрузке результатов
- Цветовая индикация статусов товаров
- Модальные окна для дополнительной информации
- Плавная прокрутка к результатам
- Обработка ошибок с понятными сообщениями

## 🎨 Дизайн

### Цветовая схема
- **Основной**: Градиент от индиго до фиолетового
- **Акцент**: Синий (#6366f1) для кнопок и ссылок
- **Успех**: Зеленый (#059669) для положительных статусов
- **Ошибка**: Красный (#dc2626) для ошибок

### Типографика
- **Шрифт**: Inter (современный, читаемый)
- **Размеры**: Адаптивная типографическая шкала
- **Веса**: От 300 (light) до 700 (bold)

## 📱 Адаптивность

Интерфейс полностью адаптивен:
- **Desktop** (1200px+) - полная сетка с несколькими колонками
- **Tablet** (768px-1199px) - адаптированная сетка
- **Mobile** (<768px) - одноколоночная компоновка

## 🔧 Настройка

### Изменение API URL
В файле `script.js` измените константу:
```javascript
const API_BASE_URL = 'http://your-api-server:port/api/v1';
```

### Кастомизация стилей
CSS переменные в `:root` позволяют легко изменить цветовую схему:
```css
:root {
    --primary-color: #6366f1;
    --accent-color: #059669;
    /* ... другие переменные */
}
```

## 🐛 Отладка

### Частые проблемы:

1. **CORS ошибки**
   - Убедитесь, что backend запущен с включенным CORS
   - Используйте HTTP сервер вместо file:// протокола

2. **API недоступен**
   - Проверьте, что backend сервис запущен на порту 8080
   - Убедитесь в правильности API_BASE_URL

3. **Заказ не найден**
   - Проверьте правильность order_uid
   - Убедитесь, что в базе есть тестовые данные

### Логирование
Откройте DevTools (F12) для просмотра:
- Сетевых запросов в Network tab
- JavaScript ошибок в Console tab
- Логов приложения в Console tab

## 🚀 Production Deployment

Для продакшен развертывания:
1. Настройте правильный API_BASE_URL
2. Минифицируйте CSS и JS (опционально)
3. Настройте HTTPS для безопасности
4. Включите gzip сжатие на веб-сервере

## 📄 Примеры использования

### Поиск заказа
1. Введите `b563feb7b2b84b6test` в поле поиска
2. Нажмите "Найти заказ" или Enter
3. Просмотрите детальную информацию

### Просмотр статистики
1. Нажмите "Статистика кеша"
2. Изучите использование кеша в модальном окне

### Список заказов
1. Нажмите "Список заказов"
2. Выберите заказ из списка для его загрузки